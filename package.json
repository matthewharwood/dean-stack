{
  "name": "dean-stack",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.13",
  "engines": {
    "bun": ">=1.3.13",
    "node": ">=22"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "check": "bash scripts/check-lockfile.sh && biome ci && stylelint --allow-empty-input --max-warnings 0 \"**/*.css\" && turbo run typecheck check:sonar test:unit --filter=@dean-stack/web && turbo run test:e2e --filter=@dean-stack/web && npx -y react-doctor@latest . --diff main && npx -y fallow@latest dead-code --fail-on-issues",
    "check:fast": "bash scripts/check-lockfile.sh && biome ci && stylelint --allow-empty-input --max-warnings 0 \"**/*.css\" && turbo run typecheck check:sonar test:unit --filter=@dean-stack/web && turbo run test:e2e:fast --filter=@dean-stack/web && npx -y react-doctor@latest . --diff main && npx -y fallow@latest dead-code --fail-on-issues",
    "build": "turbo run build",
    "dev": "turbo run dev --filter=@dean-stack/web",
    "biome:watch": "biome check --write --watch .",
    "gen:app": "turbo gen app",
    "prepare": "bash scripts/install-hooks.sh"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.0",
    "@dean-stack/biome-config": "workspace:*",
    "@dean-stack/stylelint-config": "workspace:*",
    "@dean-stack/tsconfig": "workspace:*",
    "@dreamsicle.io/stylelint-config-tailwindcss": "^1.0.0",
    "@turbo/gen": "^2.9.0",
    "chokidar-cli": "^3.0.0",
    "stylelint": "^16.0.0",
    "stylelint-config-standard": "^36.0.0",
    "turbo": "^2.5.0",
    "@typescript/native-preview": "^7.0.0-dev.20260426.1"
  }
}
